<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper 
namespace="com.itwill.spring.transaction.EmployeeMapper">

	<select id="list" resultType="com.itwill.spring.transaction.Employee">
		SELECT id, name, email, phone, salary FROM employee ORDER BY id DESC
	</select>
	<select id="getPerson" 
			parameterType="java.lang.Integer" 
			resultType="com.itwill.spring.transaction.Employee">
		SELECT id, name, email, phone, salary FROM employee WHERE id = #{id}
	</select>
	<insert id="insert" parameterType="com.itwill.spring.transaction.Employee">
		<selectKey resultType="java.lang.Integer"
			keyProperty="id" order="BEFORE">
			SELECT employee_id_seq.nextval FROM DUAL
		</selectKey>
		INSERT INTO employee (id,name, email, phone) VALUES (#{id},#{name},#{email},#{phone})
	</insert>
	<delete id="delete" parameterType="java.lang.Integer">
		DELETE FROM employee WHERE id = #{id}
	</delete>
	<update id="update" parameterType="com.itwill.spring.transaction.Employee">
		UPDATE employee SET name = #{name}, phone = #{phone}, email = #{email} WHERE id = #{id}
	</update>
	<update id="increaseSalary" parameterType="map">
		UPDATE employee SET salary = (salary + #{salary}) WHERE id = #{id}
	</update>
	
</mapper>